# Phase 2 will fill in DB load.
# Phase 1 uses in-memory seed data built directly in main.py and tests.


def load_approved_mappings(db_session=None):
    """
    Stub — Phase 2 will query DB for approved AmenityMapping rows
    and return a list of ApprovedMapping dataclass instances.
    """
    raise NotImplementedError(
        "Phase 2 will implement DB-backed mapping load. "
        "Phase 1 uses hardcoded seed data."
    )
