fastapi>=0.111.0
uvicorn[standard]>=0.29.0
sqlmodel>=0.0.18
psycopg2-binary>=2.9.9
pymysql>=1.1.0
alembic>=1.13.1
openpyxl>=3.1.2
pdfplumber>=0.11.0
python-multipart>=0.0.9
python-dotenv>=1.0.1
typer>=0.12.3
pydantic-settings>=2.2.1
pytest>=8.1.1
httpx>=0.27.0
redis>=5.0
celery[redis]>=5.4.0
flower>=2.0.0
rapidfuzz>=3.0.0
ftfy>=6.1.0
playwright>=1.45.0
pyvespa>=0.40.0
# >=1.35.60 is the floor for both the Converse API used by
# app/lobby_check/extractor.py and AWS_BEARER_TOKEN_BEDROCK bearer-token
# auth (local dev) — below it, converse() is unavailable or the token is
# silently ignored.
boto3>=1.35.60
numpy>=1.26.0
# Image framing heuristic for lobby-check (app/lobby_check/images.py).
pillow>=10.3.0
# Cost-calculation library ONLY — litellm's proxy is deliberately not used
# (see observability design doc §4). Provides a maintained Bedrock pricing
# map via litellm.cost_per_token so we don't hand-roll one.
litellm>=1.44.0
# PDF report generation for GET /api/v1/usage/report?format=pdf (§9).
reportlab>=4.1.0

# dqscan (repo-root standalone tool, run in-container via
# app/tasks/dqscan_task.py) needs these on top of what's already here
# (pymysql, openpyxl, boto3, python-dotenv, and sqlalchemy transitively via
# sqlmodel are all already covered above). croniter drives the Settings-page
# cron schedule (app/tasks/dqscan_task.check_dqscan_cron) -- dqscan itself
# doesn't use it, that's purely the in-app scheduler replacing the old host
# crontab.
PyYAML>=6.0
tabulate>=0.9
croniter>=2.0
